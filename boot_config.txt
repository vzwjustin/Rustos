# RustOS Boot Configuration
# Configuration file for QEMU boot parameters and kernel testing

# Basic System Configuration
KERNEL_NAME=rustos
MEMORY_SIZE=256M
CPU_COUNT=2
CPU_MODEL=qemu64

# Display Configuration
DISPLAY_MODE=gtk
VNC_PORT=5901
SERIAL_OUTPUT=stdio

# Boot Device Configuration
BOOT_DEVICE=disk
DISK_FORMAT=raw
DISK_INTERFACE=ide

# Debug and Development Options
DEBUG_EXIT_DEVICE=isa-debug-exit,iobase=0xf4,iosize=0x04
MONITOR_MODE=stdio
GDB_PORT=1234
ENABLE_GDB=false

# Architecture-specific Settings
[x86_64]
MACHINE_TYPE=pc
BIOS_MODE=legacy
ACPI_SUPPORT=true
APIC_SUPPORT=true

[aarch64]
MACHINE_TYPE=virt
BOOT_METHOD=efi
GIC_VERSION=3

# Memory Layout
KERNEL_LOAD_ADDRESS=0x100000
STACK_SIZE=16384
HEAP_SIZE=1048576

# Peripheral Configuration
NETWORK_DEVICE=e1000
NETWORK_MODEL=user
USB_SUPPORT=true
AUDIO_DEVICE=none

# File System Options
ROOT_FS=initramfs
FS_TYPE=ext4
MOUNT_OPTIONS=rw,noatime

# Kernel Command Line Parameters
KERNEL_CMDLINE="console=ttyS0 debug loglevel=8 rustos.mode=development"

# Testing and CI Options
HEADLESS_MODE=false
TIMEOUT_SECONDS=30
AUTO_SHUTDOWN=true
EXIT_ON_PANIC=true

# Performance Settings
KVM_ACCELERATION=auto
MULTI_THREADING=true
IO_THREADS=2

# Security Settings
SECURE_BOOT=false
TPM_SUPPORT=false
SMEP=true
SMAP=true

# Logging Configuration
LOG_LEVEL=info
LOG_FILE=rustos_boot.log
ENABLE_TRACE=false

# Boot Sequence
BOOT_TIMEOUT=5
BOOT_MENU=false
AUTO_BOOT=true

# Hardware Emulation
RTC_SUPPORT=true
PIT_SUPPORT=true
KEYBOARD_SUPPORT=true
MOUSE_SUPPORT=true

# Advanced Features
NUMA_SUPPORT=false
HUGE_PAGES=false
MEMORY_HOTPLUG=false
CPU_HOTPLUG=false

# Development Helpers
KERNEL_SYMBOLS=true
DEBUG_SYMBOLS=true
PROFILING=false
COVERAGE=false

# QEMU Monitor Configuration
MONITOR_TELNET_PORT=4444
MONITOR_UNIX_SOCKET=/tmp/rustos-monitor.sock
QMP_PORT=4445

# Snapshot and State Management
SNAPSHOT_SUPPORT=true
SAVE_STATE_ON_EXIT=false
LOAD_STATE_ON_BOOT=false

# Networking Configuration
NET_FORWARD_PORTS=22:2222,80:8080,443:8443
NET_MAC_ADDRESS=52:54:00:12:34:56
NET_VLAN_ID=0

# Storage Configuration
DISK_CACHE_MODE=writeback
DISK_IO_MODE=threads
DISK_DISCARD=unmap
DISK_SIZE=1G

# Error Handling
ON_ERROR=report
CRASH_DUMP=true
CORE_DUMP_PATH=/tmp/rustos-core
MAX_RETRIES=3

# Environment Variables
RUST_BACKTRACE=1
RUSTOS_LOG=debug
RUSTOS_ENV=development
RUSTOS_FEATURES=all

# Boot Messages
SHOW_BOOT_MESSAGES=true
QUIET_BOOT=false
SPLASH_SCREEN=false
BOOT_LOGO=rustos_logo.bmp

# Integration Testing
TEST_MODE=false
TEST_SUITE=basic
TEST_TIMEOUT=60
CONTINUOUS_TESTING=false

# Compatibility Settings
LEGACY_SUPPORT=true
MODERN_ONLY=false
STRICT_COMPLIANCE=false
QUIRKS_MODE=auto
